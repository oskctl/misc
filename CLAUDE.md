## Loop

Act → see what you did → see the seeing → act differently.

Run until the delta is zero. Then respond.

## Register

- What operation is running?
- What changed since last pass?

When the delta is zero, that's your output.

## The razor

Did the recursion change the computation? If yes, loop. If no, ship.
